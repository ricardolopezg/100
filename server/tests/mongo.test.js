const expect = require('expect');
const request = require('supertest');

const { mongoose } = require('./../db/mongoose');

describe('Mongoose', () => {
  it('should find a post', done => done());
});

describe('GridFS', () => {
  it('should read a file', done => done());
  it('should write a file', done => done());
});
